// Test file to check what render layer classes are available
import net.fabricmc.fabric.api.blockrenderlayer.v1.BlockRenderLayerMap;
import net.minecraft.client.render.RenderLayer;
// Test these imports to see what's available in Fabric 1.21.8

public class TestRenderImports {
    public void test() {
        // BlockRenderLayerMap.putBlock(someBlock, RenderLayer.getCutout());
    }
}
